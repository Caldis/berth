---
description: AI Native Workflow optimization 阶段. 读取并执行 .agents/workflow/optimization.md
argument-hint: [task-id]
---

执行 AI Native Workflow 的 `optimization` 阶段。

读取仓库根的 `.agents/workflow/optimization.md` 并严格按其执行。任务标识: $ARGUMENTS
