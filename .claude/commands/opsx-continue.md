---
description: AI Native Workflow continue 阶段. 读取并执行 .agents/workflow/continue.md
argument-hint: [task-id]
---

执行 AI Native Workflow 的 `continue` 阶段。

读取仓库根的 `.agents/workflow/continue.md` 并严格按其执行。任务标识: $ARGUMENTS
