---
description: AI Native Workflow design 阶段. 读取并执行 .agents/workflow/design.md
argument-hint: [task-id]
---

执行 AI Native Workflow 的 `design` 阶段。

读取仓库根的 `.agents/workflow/design.md` 并严格按其执行。任务标识: $ARGUMENTS
