---
description: AI Native Workflow verify 阶段. 读取并执行 .agents/workflow/verify.md
argument-hint: [task-id]
---

执行 AI Native Workflow 的 `verify` 阶段。

读取仓库根的 `.agents/workflow/verify.md` 并严格按其执行。任务标识: $ARGUMENTS
