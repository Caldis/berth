# 工程摩擦记录

> task: GH-140 冷启动首屏 SWR (3.0-implement 改 engine ensureReady, 未碰 search.ts/scope 代码)

## 发生阶段
3.0-implement 改 ensureReady SWR 后推送 → 4.0-verify CI 远端 windows e2e 红。

## 现象
GH-140 只改 engine `runtime.ts` 的 `ensureReady` (SWR: stale 立即返回旧快照 + 后台刷新), **没碰** `search.ts` / scope 代码。本地 typecheck/lint + 1237 单测 (含新 SWR 用例) + ubuntu CI 全绿, 推送。windows e2e `project-scope.e2e.ts:53` 红: `MiniSearch: duplicate ID` —— SWR 让 `search` (走 ensureReady) 在 project-scope 切换的**扫描中间态**读 snapshot, partial fold 瞬时含重复 id, `MiniSearch.addAll` 抛错。改前 search `await` 扫描完成、读无重复 final, 从不触及中间态。

**关键**: playbook `.agents/workflow/3.0-implement.md` line 20 已有规则 —— "改 runtime.search / 作用域过滤 / 搜索相关: 提交前必须本地跑 project-scope.e2e.ts" (引用 friction 20260606-scope-filter)。但我把改动判为"只改 ensureReady, 与 search 无关", 没识别 **ensureReady 是 search 的共享上游数据路径**, 规则未触发。与 archived `20260606-3.0-implement-scope-filter-broke-search-e2e` 同构 (scope 改动破坏 search e2e), 与 active `20260617-3.0-implement-e2e-check-skipped-despite-playbook-rule` 同源 (playbook e2e 规则在册未消费)。

## 工程师介入动作
1. CI 子代理报根因 (MiniSearch duplicate id @ project-scope.e2e.ts:53)。
2. `search.ts` `buildSearchDocs` 按 id 去重 (保留首个; final 仍无重复, 只防瞬时读) + `search.test.ts` dedupe 用例。
3. 本地 `pnpm build` + `playwright test project-scope` 1 passed (改前 windows 红的 spec), 推送 → CI 全绿。

## 应沉淀的上下文或规则
- **判定"搜索/作用域相关"看数据是否流经 search/scope 读路径, 不只看改了哪个文件名**: 改 `ensureReady` / runtime 数据读取共享路径 / SWR / 缓存 / await 语义, 即使没碰 search.ts，也改变了 search 看到的数据时序 → 属 line 20 的"搜索相关", 推送前本地跑 `project-scope.e2e.ts` + search e2e。
- **让派生读暴露于"扫描中间态/部分态"的改动 (SWR、流式 partial、stale-serve), 必须验证消费方对中间态健壮**: search 要求 unique id (重复即崩); dashboard/usage 计数会瞬时偏差 (可接受, scan 完成 reload 修正)。改这类时主动列出所有派生消费方, 逐个问"中间态喂进去会怎样"。
- **认知锚**: engine 单方法改动的 blast radius 不止其文件 —— 被多个派生 IPC 共用的读路径 (ensureReady 被 search/scope/sessions/insights/usage/health 共用), 行为变更会沿所有消费方传导到 e2e。

## 建议的流程改进
(由 harness-5.1-friction 消费)
- playbook `3.0-implement.md` line 20 "搜索相关"补界定: 改 `ensureReady` / runtime 共享读路径 / SWR / 缓存语义也算, 推送前本地跑 project-scope.e2e.ts + search e2e。
- 与 20260617 合并信号: 连续两个 task 因"playbook e2e 规则在册未消费/边界没识别"复发 → 5.1 应把"改 X 必跑受影响 e2e"从文字升级为 prepush 可检测闸门 (staged 含 engine 数据读路径 / renderer 页面 chrome → 阻断式提醒"已本地跑受影响 e2e?")。
