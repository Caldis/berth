# 工程摩擦记录

> 命名: docs/friction/{YYYYMMDD}-{action-id}-{summary}.md
> action-id: 0.0-new|0.1-continue|1.0-explore|2.0-design|3.0-implement|3.1-polish|4.0-verify|5.0-archive|5.1-friction|5.2-issues

## 发生阶段

GH-86 `harness-2.0-design` 提交推送后的 CI wait。

## 现象

共享规则要求运行:

```bash
pnpm harness:ci:wait -- --sha a1d99171d52e31e17371fb22cfd45ae82c6be8f8
```

实际脚本收到一个额外 `--`, 报错:

```text
unknown option: --
```

可用形式为:

```bash
pnpm harness:ci:wait --sha a1d99171d52e31e17371fb22cfd45ae82c6be8f8
```

该命令成功等待 CI#26859451850 并通过。

## 工程师介入动作

- 保留同一 SHA。
- 改用 `pnpm harness:ci:wait --sha <full-sha>`。
- 确认 CI run `26859451850` completed/success。

## 应沉淀的上下文或规则

`harness-ci-gate.mjs` 当前不吞掉 pnpm 传入的分隔符 `--`。workflow 文案与脚本解析需要一致。

## 建议的流程改进

1. 修改 `harness-ci-gate.mjs` 参数解析, 忽略单独的 `--`。
2. 或把 `.agents/workflow/_shared.md` 中的命令改为 `pnpm harness:ci:wait --sha <full-sha>`。
3. 增加 harness CI gate 参数解析测试, 覆盖两种调用形式。

## 复发记录

- 2026-06-06 (GH-109 archive): 复发。`_shared.md` 仍写 `pnpm harness:ci:wait -- --sha`, 照做再次得到 `unknown option: --`, CI wait 未真正等待 (脚本立即报错退出)。改用 `pnpm harness:ci:wait --sha <full-sha>` 后正常。
- 当轮已落实改进 #2: 把 `.agents/workflow/_shared.md` 的 ci:wait 命令文案改为不带 `--` 的形式, 杜绝照抄踩坑。#1/#3 (脚本忽略 `--` + 参数解析测试) 留待 tooling 专项。
