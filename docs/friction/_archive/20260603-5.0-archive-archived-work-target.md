# 工程摩擦记录

## 发生阶段
5.0-archive

## 现象
GH-77 归档后, 运行 `pnpm harness:check --work docs/works/_archive/2026-06-02-gh-77-project-scope-switcher` 失败, 报 `--work target not found "2026-06-02-gh-77-project-scope-switcher"`。

## 工程师介入动作
改为运行全量 `pnpm harness:check`, 结果通过。GitHub Project 状态检查和 `harness:stats` 也通过。

## 应沉淀的上下文或规则
归档流程在 `git mv` 之后不能继续假设 `--work` 支持 `_archive` 路径。若工具暂不支持归档目录, archive 阶段应明确在移动目录后使用全量 `pnpm harness:check`。

## 建议的流程改进
让 `harness-check --work` 支持 `docs/works/_archive/{task}` 路径, 或修改 `.agents/workflow/5.0-archive.md` 的本地归档检查说明, 避免移动后仍给出不可用的窄范围检查命令。
