# 工程摩擦记录

> GH-115 T1 把 preload 改为按 IpcChannels/IpcEvents 表派生时, `window:maximized-change` 按表声明的 `{maximized: boolean}` 解包, 实际线上载荷是裸 boolean — 最大化按钮状态失效, windows e2e (window-controls) 抓出, T1 提交推红 CI。

## 发生阶段
3.0-implement (GH-115 T1→T2 之间, CI run 27267310835)。

## 现象
契约表本身就是漂移产物 (01-ANALYSIS R3 的结论), 重构时却把表当真源对齐实现 — 表错则实现跟着错。本地 typecheck/lint/vitest 全绿 (单测不过真 IPC 线), 只有 e2e 才暴露。三个事件里 assets:changed/assets:progress 我核验过实发 site, 唯独 maximized-change 信了表。

## 工程师介入动作
无 (CI 拦截, Agent 自修), 代价: 一次红灯 + 修复提交。

## 应沉淀的上下文或规则
- **把"自称契约"升级为"强制契约"的重构, 方向必须是 表 ← 实现/线上行为, 逐条对实发/实收 site 核验后改表; 任何一条凭表改实现 = 把历史漂移固化成回归。**
- 推送事件类改动属于"数据流/时序"类 (memory: runtime-behavior-needs-real-run): 改完 preload 解包逻辑, 本地至少跑一次相关 e2e (pnpm build && pnpm test:e2e window-controls) 或真启动点按验证, 不依赖远端 CI 当第一道网。
- 已在 ipc.ts IpcEvents 注释里写明三个事件的实发 site 与"改表先查 send 调用点"。

## 建议的流程改进
- 契约对账测试 (ipc-contract.test.ts) 只对账通道名集合, 对不了 payload 形状; payload 形状的真源核验进 checklist: 改 IpcEvents 任何 entry 时 grep `webContents.send('<event>'` 逐个核对第二参数。
