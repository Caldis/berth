# 工程摩擦记录

> 命名: docs/friction/{YYYYMMDD}-{action-id}-{summary}.md
> action-id: 0.0-new|0.1-continue|1.0-explore|2.0-design|3.0-implement|3.1-polish|4.0-verify|5.0-archive|5.1-friction|5.2-issues

## 发生阶段

GH-86 `harness-1.0-explore` 阶段。

## 现象

执行 `pnpm harness:check --work docs/works/2026-06-03-gh-86-app-performance-analysis` 时, 当前任务目录没有被指出结构错误, 但命令仍失败:

- `issues: root directory named issues is deprecated; use docs/issues/`
- `.agents/skills/opsx-*` distribution drift

这与 `.agents/tools.md` 中“并行任务影响全局检查时, 用 `--work` 只验证当前任务目录”的说明不一致。

## 工程师介入动作

- 读取 `scripts/harness-check.mjs`, 确认 `checkWorks(root, { work })` 会按 work 过滤任务目录。
- 同一命令仍执行全局 issues 与 distribution 检查, 因而 scoped work 验证被无关全局状态阻断。
- 本任务未修改根 `issues/`、`.agents/skills/opsx-*` 或其他 Agent 的 `GH-87` 目录。

## 应沉淀的上下文或规则

当 `--work` 存在时, harness check 至少应提供“仅当前 work 结构校验”的可用模式, 或明确说明 `--work` 仍会检查全局 issues/distribution。

## 建议的流程改进

1. 在 `scripts/harness-check.mjs` 中区分 `--work` scoped 检查与全局检查。
2. 若仍需要全局检查, 为并行工作区提供显式参数, 例如 `--work ... --skip-global-distribution`。
3. 更新 `.agents/tools.md` 与 workflow 中关于 `pnpm harness:check --work` 的描述, 避免阶段提交被无关 drift 阻断。
