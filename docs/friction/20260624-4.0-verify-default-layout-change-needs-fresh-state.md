# 工程摩擦记录

task: 2026-06-23-gh-150-widget-height-tiers · issue GH-150

## 发生阶段
4.0-verify (dashboard 各 widget 默认高度 CDP 实测校准)

## 现象
CDP 实测 widget 卡高/裁切 (clip) 反复拿到失真数据, 多轮校准白跑:
- 改 `widget-catalog` 默认 h / `dashboard-layout` migrate 后, 跑起来卡高仍是旧值。根因: HMR 保留 React layout state, 且 layout 持久化在 `localStorage['berth-dashboard-layout']`, 二者都不会因 catalog 改动重算 → 测到的是旧默认。
- 新建 agent-dev 实例 profile 是空的 (走 defaultLayout 新 catalog, 这点对), 但首扫数据 20-40s+; 固定 `waitForTimeout(28s)` 不够 → 测到 loading 骨架。骨架是定高的, clip 恒 0, 给出"全部贴合"的假绿 (真实数据下 insights 241 / recent-sessions 363 才暴露裁切)。
- compaction 摘要断言"Read 不可靠 / Write 可靠 / 必须用 python / 本地周期 reset 到基线"——全是上一轮调试的幻觉结论, 实测 Read/Edit/commit 均正常, 据此绕路纯属浪费。

## 工程师介入动作
- 验证默认布局/catalog 变更: 先 `localStorage.removeItem('berth-dashboard-layout')` + `page.reload()`, 再**轮询 `section .animate-pulse` 骨架清零**才测 (而非固定 sleep)。
- 真实内容高用 `grid.style.display='block'` 临时去除 grid-stretch 后量 `inner.scrollHeight`; clip 用 `inner.scrollHeight - grid.clientHeight`。
- 门禁以 `pnpm typecheck` / `pnpm test` 实跑为准; IDE 诊断对 test 文件的 `@/`·`@shared` alias 与 jest-dom matcher 常误报, 忽略。
- CDP 脚本结束用 `process.exit(0)` 断开, **不要 `browser.close()`** (connectOverCDP 下会关掉 Electron 窗口, 下次连接 "no page")。
- 调试端口被占报 `Cannot start http server for devtools`: `lsof -ti tcp:<port> | xargs kill -9` 或直接换端口。

## 应沉淀的上下文或规则
已并入 memory `berth-ui-visual-verify-on-mac` (验证默认布局变更的 storage-clear + 骨架轮询 + process.exit 三条)。语义类高度/裁切是动态产物, 静态绿与骨架态都证明不了 (见 memory `runtime-behavior-needs-real-run`)。

## 建议的流程改进
(由 harness-5.1-friction 消费) 默认布局 / catalog / migrate 类 UI 变更, 验收脚本固定套路: 清 storage → reload → 轮询骨架清零 → 去 stretch 量自然高 + 量 clip → process.exit。一次性低复发, 留档不折常驻规则。
