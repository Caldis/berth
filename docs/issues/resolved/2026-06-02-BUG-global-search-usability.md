# 描述
- 当前搜索功能不可用或可用性很差, 用户无法依赖它找到目标内容。

# 重现步骤
- 打开全局搜索。
- 输入已知存在的会话、配置、命令、技能或 MCP 关键词。

# 预期结果
- 搜索能覆盖主要实体, 返回可点击、可辨识来源的结果。
- 空结果、加载和错误状态清楚。
- 键盘操作可用。

# 实际结果
- 当前搜索被用户判断为“完全是废的”。

# 解决方案
- 先审计搜索索引、查询入口和结果 UI。
- 定义搜索范围: sessions / projects / configs / hooks / skills / MCP。
- 从一个高价值范围开始修复, 再扩展到其他实体。
