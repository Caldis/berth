# 任务清单 (Design 产物 / 活清单)

从 02-SPEC 拆解。**主 Agent 主循环顺序执行** (订阅额度触顶, 不 fan-out 子代理); 顺序即依赖序。
每批: 先写/更测试 → 实现 → `pnpm typecheck && pnpm lint && pnpm test` 绿 → 只暂存本批文件 `git diff --cached` 核对 → 提交。
热点纪律: 触碰 verify 任务足迹文件前 `git log --since=2026-06-08 -- <file>` 确认近 48h 无并行写入; gh-103 足迹 (chart-colors/globals.css/overview.tsx/usage.tsx/token-usage-display) 一律延后到 T13 且仅限非 chart 段, 冲突则改 defer 声明。

- [x] T0 测试地基 (2026-06-10 done: tsconfig.test.json 渐进纳管 109/133 文件, 24 文件 69 错进 exclude 账本; parsers.test.ts 假覆盖删除; worker-artifact 三方一致性测试; abi-smoke 本地跑通; CI 矩阵 +macos (project-scope e2e 按已知 issue 文件内 skip); redaction 枚举 5 用例绿。门禁: typecheck×3 + lint + vitest 全量绿 (4 个渲染测试在外部负载下 flaky, 默认隔离重跑 68/68 绿, 见 friction 20260610): tsconfig.test.json (tsc dry-run, >15 文件错误则按目录渐进 include) + 删 tests/unit/parsers.test.ts 假覆盖 + tests/unit/worker-artifact.test.ts + scripts/abi-smoke.mjs + CI e2e 矩阵加 macos-latest (project-scope e2e 红则按已立案 issue test.skip 交叉引用) + tests/renderer/status-line-redaction.test.tsx (capabilities.tsx 仅加 export 关键字)
  - files: tsconfig.test.json(新) tsconfig.json tests/unit/{parsers.test.ts删,worker-artifact.test.ts新} scripts/abi-smoke.mjs(新) .github/workflows/ci.yml tests/renderer/status-line-redaction.test.tsx(新) src/renderer/src/pages/capabilities.tsx(仅 export)
  - tests: 本批即测试; redaction 枚举 (env var / inline sk-key / url token / 纯命令 / 空)
  - verify: pnpm typecheck (含新 project) + pnpm test 全绿; abi-smoke 本地跑通; CI 双 OS 绿; capabilities.tsx 是 gh-104 足迹 → 仅 export 关键字一行, 提交注明
- [x] T1 IPC 契约单源派生 + 四方对账网 (2026-06-10 done: IpcChannels 表修正为 35 通道照实 (补 snapshot/status/refresh) + 删幽灵事件 scan:progress + assets:changed 改实发 WatchEvent; preload 全通道改 typed invoke/subscribe, 复杂参数直接从表派生; index.d.ts 104 行手写镜像 → 13 行 BerthAPI 派生; setup.ts 导出 mockApi 并补 memory/setScope mock (对账抓出的真实缺口); ipc-contract.test.ts 四方对账 5 断言绿; 自证完成: 注入伪通道即红 2 项后撤销。satisfies BerthAPI 全等收紧推迟到 T2 (phantom 清理后), 避免本批触碰 hooks-lifecycle-view (gh-104 足迹)。门禁: typecheck×3/lint/vitest 914 全绿): preload export type BerthAPI = typeof api; index.d.ts 改派生; IpcChannels/IpcEvents 表修正为真实集合 (现役 35 通道 + knownDead 白名单钉脏现状); tests/setup.ts mock 加 satisfies; tests/unit/ipc-contract.test.ts 四方对账 (handlers==preload==IpcChannels==mock)
  - tests: ipc-contract.test.ts 先行; 自证: 临时注入未注册通道确认变红后撤销 (verify 记录输出)
  - verify: 对账绿 + 自证红样本留存 plan 注记; window.api 运行时形状零变化 (grep diff)
- [x] T2 IPC 死面整链删除 + **T1 推送事件回归修复** (2026-06-10 done: ① CI 红灯修复 — window:maximized-change 实发裸 boolean, T1 信表解包 {maximized} 致 windows e2e 红, 表照实改 boolean + preload 直传 (friction: contract-table-trust-broke-wire); ② 7 死通道整链删 (theme:get / hooks:status / hooks:set-enabled / assets:scan-all 全链 + relations / import-chain / mcp:merged handler-only, 连带 computeMcpMerged 38 行 + MCPMergeInfo + HooksEnablementStatus/SetHooks* 类型; relations.ts 本体保留 — manifest 守卫触发: cli-args.ts:26 声明 relations flag + packages 测试在用); ③ phantom assets.scan 删; ④ hooks-manager 死函数族 9 个删 (~150 行, setHookEnabled 链保留); ⑤ PlatformInfo.claudeDir 删 (81 处同名命中确认为 scanner 测试 fixture 别概念); ⑥ mock 键结构 satisfies BerthAPI (方法集合编译期全等, 返回值类型留 exclude 账本); ⑦ 对账白名单清零→严格全等; ⑧ projectReadOnly i18n key 双语删; ⑨ hooks-lifecycle-view 幻影断言删 + 出 exclude 账本 (23 文件)。门禁: typecheck×3/lint 绿, vitest 909/910 (settings-dialog Escape 焦点项为外部负载 flake, 隔离重跑 4/4 绿): theme:get / hooks:status / hooks:set-enabled / assets:scan-all 全链 (handler+preload+d.ts+mock) + phantom assets.scan + handler-only assets:relations / assets:import-chain / mcp:merged (删 handler + computeMcpMerged + MCPMergeInfo; **relations.ts 本体保留** — cli-args.ts:26 manifest 守卫触发, packages 测试在用) + hooks 复数残段 (getAgentHooksStatuses + d.ts statuses + mock + hooks-lifecycle-view 幻影断言 + projectReadOnly key) + PlatformInfo 收敛 (删 claudeDir)
  - tests: 白名单清零断言 (T1 网先红后绿); hooks-lifecycle-view.test 同步
  - verify: 全量绿 + 通道名字符串 grep 零残留; handlers/preload/ipc.ts 是 gh-86/95/104 足迹 → 触碰前 git log 确认
- [x] T3 渲染层死纵切片删除 (2026-06-10 done: 8 文件删 — asset-card/stat-card/tab-group 第一代孤儿三件套 + asset-guidance 簇 (lib+panel+2 保活测试) + local-sources-section; useScanSources hook 删; local-source-copy 迁 layout/ (唯一活消费者旁); i18n 独占 key 删 12 个双语 — 执行中发现并修正两点: ① capabilities.guidance.docs.codexConfig 与死 key 同名但双引用存活, 行级删除误删后已恢复; ② json.dump 重排会摧毁 locale 紧凑内联格式, 必须行级手术删除 (差点污染 churn 榜首文件 208 行)。门禁: typecheck×3/lint/vitest 906 全绿): components/shared/{asset-card,stat-card,tab-group}.tsx + asset-guide-panel.tsx + lib/asset-guidance.ts + 保活测试 (asset-guide-panel.test.tsx / asset-guidance.test.ts) + settings/local-sources-section.tsx + useScanSources (use-ipc.ts:469) + local-source-copy.ts 迁 layout/ + 独占 i18n key 家族 (settings.sourceCategories/sourceScopes/scanDirectories/sourceDetected + instructions.guidance.docs.codexConfig)
  - tests: 删保活测试; settings 页既有测试确认 LocalSources 不再被引用; vitest 全量
  - verify: grep 零残留 (裸名+路径+@/ 别名); assets:scan-sources 通道保留 (scope-switcher 在用); lib/asset-guidance 是 gh-94 足迹 → git log 确认; 截图: settings 页前后 diff
- [x] T4 store 死字段 + fold 单写路径 + 全局错误边界 (2026-06-10 done: AppState 删 5 组死状态 (recentSessions/usageSummary/agentDetected 三组字段+action, setScanning, lastAssetRefreshAt) + scanning 反规范化副本删除并在 2 个读点改为 assetRuntimeStatus.state 派生; setAssets/setStats 裸替换 action 删除, selectScope 单走 setAssetSnapshot (fold 不变量类型层不可绕); agentView 不动 (已立案待产品决策); PageErrorBoundary 全路由默认兜底 + 返回总览脱困动作 (test-first 红→绿) + common.backToOverview 双语 key; 6 个测试文件 setState 字面量同步收缩。门禁: typecheck×3 0 错/lint/vitest 907 全绿。**偏差记录**: 各批 CDP 真跑场景 (T4 fold/边界、T6 吞错、T9 watch 链、T10 session 页) 合并到 4.0-verify 一次实测覆盖, 中间批以静态门禁+单测护住): 删 recentSessions/usageSummary/agentDetected/setScanning/lastAssetRefreshAt (agentView 不动, 已立案待产品决策); selectScope 删 setAssets/setStats 裸写改单走 setAssetSnapshot 并删两 action; AppLayout 全路由 PageErrorBoundary + "返回 Overview" 动作 + en/zh boundary key (B 嫁接: 仅 boundary 前缀, 与 verify 任务写入面零交集)
  - tests: app-store.test 收缩 + selectScope 单写断言; page-error-boundary.test 扩全路由+动作; capabilities-plugins/scanning-empty 测试 setState 字面量同步
  - verify: CDP 真跑项目切换 — 观察无 shallow 资产瞬时丢失/闪烁 (fold 不变量); 人为抛渲染错误 → 边界呈现 + 返回动作可用; 暗色截图
- [x] T5 主进程可观测性地基 (2026-06-10 done: src/main/log.ts 纯 fs 工厂 (滚动 1MB 保一代, 自身永不抛, 仅落 userData/logs 无遥测) + setMainLogWriter 组合根装配 seam; index.ts installProcessGuards (uncaughtException 弹框+落盘 / unhandledRejection / render-process-gone) + whenReady .catch (启动失败弹框替代无声无窗口) ; AssetWatcher 增 setErrorListener — chokidar error 与 listener 抛错统一出口 (applyWatchEvent 失败不再直冲 uncaught); tests/unit/main-log.test.ts 4 用例 (格式/滚动/容错/error seam)。**附带 T4 红灯修复**: page-error-boundary 测试在 React 19 并发渲染下恢复错误逃逸为 Unhandled Error (用例全过但 run exit 1), 加 window error preventDefault 拦截; 工艺违规复盘见 friction gate-race-and-partial-rerun。门禁: tc/lint/build/vitest 全量): src/main/log.ts (userData/logs 滚动 ~50 行, 无网络出口) + uncaughtException/unhandledRejection/render-process-gone 钩子 + whenReady .catch + dialog.showErrorBox + watcher.on('error') + applyWatchEvent try/catch
  - tests: tests/unit/main-log.test.ts (滚动/格式); watcher error 监听单测 (mock emitter)
  - verify: pnpm build 冒烟 (装配段) + dev 启动看 logs 目录落盘; index.ts 是 gh-96 足迹 → git log 确认
- [x] T6 吞错簇修复 (2026-06-10 done: ① parseMcpServers/parseClaudeJsonProjectMcp 内层 catch 删除 — malformed JSON 上抛给已包好的 safeScan 记账 (existsSync 在调用侧先行; 红→绿: 坏 .claude.json fixture → ScanError 断言); ② shallow-conventions ×2 / derive-asset ×2 既有降级 catch 注入 log seam (降级语义保留, 增可观测); ③ runtime 故障汇聚点先落盘再转 status (stack 不再一跳即丢); ④ sqlite store 三处 best-effort catch 落盘 (首败永久放弃不再零痕迹); ⑤ engine/scanner detect 异常留痕 (renderer 级未安装/检测失败区分随 adapter 契约扩展, 本批不改契约); ⑥ ScanProgressPanel 渲染 status.error (零渲染→可见, testid 断言)。log.ts 定位为 main 根级中立件 (electron-free), engine 可依赖 — ARCHITECTURE 更新时写明。门禁: tc 0 错/lint/vitest 913 全绿 exit 0): 删 parsers 内层裸 catch (parseMcpServers 等) 让 safeScan 记账接管 + detect 异常 push ScanError (区分未安装/检测失败) + runtime catch 接 log seam + sidebar-scan-status 渲染 status.error + sqlite 首败日志; **不含 pricing/catalog.ts** (评审证伪其失败路径)
  - tests: 坏 .mcp.json fixture → ScanError 记账断言 (先红后绿); detect 异常用例; codex 侧不改记账语义 (R17 deferred)
  - verify: 真跑手工写坏 ~/.mcp.json 观察 UI 记账提示恢复后消失; parsers/scanner/runtime 三热点 → git log 确认 + 单独提交
- [x] T7 纯助手单源化 (2026-06-10 done: src/shared/object-guards.ts 单源 (isRecord 族 9 函数 + readStringArray (handlers 严格语义) + extractAtImports); parser-helpers/markdown 改 re-export 保持 import 面; handlers 本地 5 副本删 (slugId→safeId); health extractAtImports 逐字副本删; agent-teams 跨模块违例改 @shared; path-utils 增 isPathInside({includeEqual,platform}) — memory ×2 (equal=inside) 与 scanner (samePath||inside 复合) 三处收敛, win32 折叠对齐 samePath 先例, scanner 本地 normalizePath 随删; memory 两份 splitFrontmatter **只钉 characterization 5 用例不收敛** (归属 engine-shared-core-package); manifest.ts 本地副本**不动** (readString 带 trim 返回, 语义有差非逐字等价)。新测试 3 文件 18 用例。门禁: tc 0/lint/vitest 931 全绿 exit 0): src/shared/object-guards.ts (isRecord/readString/readNumber/safeId/extractAtImports) + parser-helpers 改 re-export + health/memory/agent-teams 副本改 import + path-utils 增 isPathInside({includeEqual})/dedupePaths 收敛 4 套 (per-call-site 保现行为) + memory 两份 splitFrontmatter **只钉 characterization 不收敛**
  - tests: object-guards.test 新 + path-utils.test 扩矩阵 + memory splitFrontmatter characterization ×2 (含 5 点差异钉死)
  - verify: 全量绿; 行为零变化 (纯 import 重定向)
- [x] T8 adapter 契约收紧 + descriptors 解环 (2026-06-10 done: AgentAdapter 删 scanAssets/watchAssets/resolveRelations 3 死方法 + 两实现桩 (~90 行, 注释标记真实缺口契约化归 engine 包 issue); per-agent descriptor 数据下沉 adapters/{claude-code,codex}/descriptors.ts + helper 进 adapters/_shared/source-descriptors.ts, agent-plugins/descriptors.ts 改聚合 re-export — **唯一模块级值依赖环解除** (adapters 零 import agent-plugins, grep 验证); relations.ts extractAtImports 改 @shared (消 1 条 engine→adapters 直连)。**偏差**: capability map 单点分发移到 T9 随源表一起建 (先建空骨架属投机抽象); 门禁: tc/lint/vitest 931 + scan-engine 包 24 全绿): 删 AgentAdapter 3 死方法 + 两实现桩; per-agent descriptors 数据下沉 adapters/{claude-code,codex}/descriptors.ts, agent-plugins 聚合 re-export (解值依赖环); 新建 engine/agent-capabilities.ts 单点 map, engine 7 处直连 (health/shallow-conventions/derive-asset/watcher/relations/handlers) 改经 map (直连降为 1 处)
  - tests: agent-capabilities.test 新 (两 agent 全能力分发等价); adapter/registry 既有测试收缩; descriptors drift 既有测试迁移
  - verify: 全量绿 + import 方向 grep (adapters 零 import agent-plugins); scan 输出 snapshot 等价
- [x] T9 扫描源声明式单表 + capability map (2026-06-10 done: adapters/{claude-code,codex}/sources.ts per-agent 声明 (glob 4+2 / file 3+2 / watch 3+3) + adapters/_shared/project-sources.ts 类型; engine/agent-capabilities.ts 单点聚合漏斗 — shallow CAPABILITY_GLOBS+FILES / derive cap-1+cap-2 两张 DISPATCH / watcher 项目段全部改由其派生, engine→adapters 的 capability 直连从 3 文件并入 1 文件 (T8 的 capability map 在此落地); 等价钉测先行: fixture 11 文件 ×3 消费方枚举输出钉死, 接表前后零 diff; **范围澄清**: conventions 两表 (SHALLOW_SOURCES / CONVENTION_DISPATCH) 是有意的表示模型分叉 (shallow 单资产+readByAgentIds vs derive 双 agent 双资产) 不并入, 注释互指; adapter scanAll 散落调用不动 (settings.local.json 2/5-parser 分叉如实保留, 归 engine 包 issue); 新增 agent = sources.ts + agent-capabilities 登记一行。门禁: tc/lint/vitest 934 + 包 24 全绿): adapters/{claude-code,codex}/sources.ts 声明; shallow-conventions CAPABILITY_GLOBS / derive-asset DISPATCH / watcher getAssetWatchPaths 三方接表; settings.local.json 分叉 per-consumer 如实建模不选边; scanner scanAll 不动
  - tests: asset-sources.test 新 — 接表前后三方枚举输出 diff 为空 (等价钉测先行)
  - verify: 真跑新建 ~/.claude/skills/test-skill 观察 watch→derive→UI 增量全链 (CDP); 全量绿
- [x] T10a handlers 瘦身 — session 域迁移 (2026-06-10 done: engine/session-detail.ts 迁入 ~218 行域逻辑 (toSessionSummary/模型知识库 KNOWN_MODEL_METADATA/infer 族/named-assets/hook events/execution detail) + buildSessionDetail 编排; sessions:get 压成单调用; runtime 字节级 toSessionSummary 副本删除改单源 import (顺带发现并清 runtime 第 8 份标量守卫副本); session-activity.ts git mv 进 engine (ipc←engine 反向边预防); handlers 548→178 行回归薄门面; golden 钉测 6 用例一次绿 (域逻辑首次可直测 — 此前 electron 顶层 import 致全仓零触达)。门禁: tc 0/lint/vitest 940 全绿)
- [x] T10b home 归一 + 注册按域拆分 (2026-06-10 done: ① listAgentTeams 默认遍历 resolveClaudeDirs (BERTH_EXTRA_CLAUDE_DIRS 契约修复, 参数保留注入口, 多 home 聚合测试红→绿); ② ClaudeNativeSource 多 projects 根 (构造 string|string[] 向后兼容, (root,slug) 对内部线程化, localId 跨根首中) 默认 resolveClaudeDirs; ③ handlers 注册按域拆五函数 (window/system/asset/session/domain), ipc/index.ts 统一装配; ④ ipc-registration.test (vi.mock electron): 运行时注册集合 == 契约表键集 — 与文本对账互补的运行时事实网。门禁: tc 0/lint/vitest 942 全绿): engine/session-detail.ts 迁入模型知识库与 sessions:get 编排 (golden 钉测先行); toSessionSummary 收敛 runtime 单实现; 注册按域拆分 (registerAssetHandlers → 域函数); memory/agent-teams/handlers 的 os.homedir 拼接改 resolveClaudeDirs (BERTH_EXTRA_CLAUDE_DIRS 契约修复, 构造参数保留); handlers 薄注册测试 (vi.mock electron); pricing 深路径 import 收回 barrel
  - tests: session-detail.test golden 先行; memory/teams 测试扩 EXTRA_DIRS 用例 (先红后绿); ipc-registration.test 新
  - verify: sessions:get 输出字节级等价 (golden diff); CDP session-detail/teams 页; handlers 是 gh-86/95/104 足迹 → git log 确认
- [x] T11 打包面收敛 (2026-06-10 done: 7 个 renderer 库移 devDependencies (deps 收敛为 8 个 main/preload 真实运行时依赖); electron-builder files 黑名单→加法白名单 ['out/**','package.json','LICENSE'] + 删 asarUnpack resources (smartUnpack 自动处理实证)/publish 占位/死排除; components.json 删 + README shadcn→HeroUI ×2; lockfile 重建 (清幽灵 @radix-ui 残链需 rm -rf node_modules)。**验收 (asar 实测)**: 2686→888 条 (-67%), website/scripts/.agents/.claude/heroui/framer-motion/zustand/lock 全零泄漏, out/* + better-sqlite3 + unpacked 完好; 产物启动冒烟 8s 存活; abi-smoke 绿。**旁支实证**: 本机有签名身份时 inert entitlements 配置是打包阻断项 (非跳过), 已追记 macos-signing issue, 旁路 CSC_IDENTITY_AUTO_DISCOVERY=false。门禁: tc 0/lint/vitest 942 全绿): 7 renderer 库移 devDependencies; electron-builder files 改加法白名单 ['out/**','package.json','LICENSE']; 删死配置簇 (asarUnpack resources / publish 占位 / !PLANS / 死排除条目); components.json 删 + README shadcn 两处修正; app icons 不动 (T14 立 issue 决策接线或删)
  - tests: worker-artifact.test (T0 已建) 守 build 产物; asar list 前后 diff 留档 plan 注记
  - verify: pnpm package:mac 本地打包 + asar list 对比 (死包消失/必需保留) + 产物启动冒烟
- [ ] T12 i18n 死 key + zh 复数收敛 (末位, churn 榜首避撞): 24 死 key 按前缀分 3 commit 删 (en/zh 双份); zh 复数 9 组收敛 _other; i18n-plural-convention.test.ts 新
  - tests: plural-convention 结构断言; 每 commit 前该前缀 grep 零引用 + git log 确认无并行写入
  - verify: 双语启动冒烟 + 全量绿
- [ ] T13 长尾孤儿收尾: 死导出 9 处 (tokenUsageTotal 在 **src/shared/token-usage.ts** 路径修正, 测试降级; assetProjectPath 仅摘名; codex shim 连带 import-rename) + JSON createSnapshotStore (stripRaw 原位保留) + getAgentHooksStatuses 残段若 T2 未尽 + **globals.css 死 token + tailwind keyframes: 仅当 gh-103 已收口** (届时 theme-palette.test 整体改写为行为断言 — C 嫁接; 否则 defer 声明留 T14 issue)
  - tests: 连带测试/mock 同步; vitest 全量
  - verify: grep 零残留; pnpm build
- [ ] T14 收尾与沉淀: deferred 14 项立案/增补 (新立: health-restructure-and-message-contract / window-hardening / god-pages / session-id-contract / expandable-asset-card / renderer-dir-semantics / app-icon-decision; 增补: engine-shared-core-package / renderer-cached-resource-hook / shared-path-and-type-config / heroui-followup / macos-signing); docs/ARCHITECTURE.md 更新 (分层表 + 12 规则 + 例外清单含 health 直连过渡态); _roadmap.md 同步; INDEX debt.final 回填
  - tests: pnpm harness:check + harness:issues
  - verify: 全部 issue 过闸门; ARCHITECTURE 与代码实况一致 (抽查 import)

## verify 回写
verify 不通过项作为新任务追加于此, phase 退回 implement。
