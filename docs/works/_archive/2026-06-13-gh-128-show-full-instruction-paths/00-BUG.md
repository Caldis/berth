# BUG 快照

用户反馈:

> 然后还有就是像约定页的这种路径，反斜杠的这几个 case 我都会看到它的路径被省略号缩写掉。这里对我理解完整路径造成很大困难，请优化这里显示完整路径

上下文:
- GH-127 已修复正斜杠和反斜杠混用, 但长路径仍通过 `truncatePath()` 显示为 `D:\...\project\CLAUDE.md` 这类中间省略格式。
- 用户需要在约定页这类路径列表里看到完整路径, 不能因为省略号丢失路径主体信息。

目标:
- 检查约定页和同类指令路径展示。
- 优化路径显示, 让完整路径可见且不产生横向溢出。
- 补测试防止长 Windows 路径重新被省略号缩写。
