# PRD 快照 (只读)

> 原始 PRD 的快照。任何阶段不回写。来源链接/页面 ID 记于此。

来源:
- 用户在 Codex 会话中提出的 Hooks 页面调整请求, GitHub Issue: https://github.com/Caldis/berth/issues/10

## 正文
目前 hooks 里面的这个功能区只有生命周期/对照 Agent 这个视图切换有一点用, 健康检查功能如果目前实现了, 就放着, 没实现就删掉, 全部禁用的功能目前不需要, 舒适/紧凑切换也没有乱用, 删掉.

然后需要左侧生命周期的部分 sticky 在页面中, 作为一个边栏部分固定展示在页面内, 确保不跟着页面上下滚动, 这个是高频交互区域。
