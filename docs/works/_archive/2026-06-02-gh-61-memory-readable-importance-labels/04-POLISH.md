# Polish

可选抛光阶段记录。只在用户主动要求, 或 Agent 在复杂任务 verify 后询问并得到同意时填写。

## 当前任务边界
只调整 Memory 页 importance badge 和 filter chip 的短文案，不改变筛选、展开、刷新和数据结构。

## 候选项
- badge/filter 显示本地化 label。
- badge hover 保留完整加载语义说明。
- 继续复用现有 chip/badge 样式，避免额外占用空间。

## 用户选择
本轮采用上述小抛光项；无需追加页面重排。
