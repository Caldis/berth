# Polish

可选抛光阶段记录。只在用户主动要求, 或 Agent 在复杂任务 verify 后询问并得到同意时填写。

## 当前任务边界
- 只检查 GH-85 相关的项目范围切换器与 Settings 页面来源入口。
- 不扩大到新的项目详情页, 不改主进程扫描契约。

## 候选项
- 项目范围下拉中项目来源明细的高度、路径裁剪和滚动行为。
- Settings 页面移除 Local Sources 后的空白结构。

## 用户选择
- 本轮按用户对 UI/UX 的长期要求执行收尾视觉检查, 不新增单独功能。

## 已处理
- 通过 dev agent + CDP 确认项目候选行展示来源摘要, 选中 `berth` 后显示 Claude Code / Codex 项目来源明细。
- 通过 CDP 确认 Settings 页面不再出现 `Local Sources` / `本地来源` 旧区块。
- `print-window` 首次截图发现项目范围弹层高度过大, 顶部被窗口裁切。
- 已将项目范围弹层限制为窗口内最大高度并启用内部滚动, 第二次截图确认顶部留有空间, 长路径不撑宽。
