# PRD 快照 (只读)

> 原始 PRD 的快照。任何阶段不回写。来源链接/页面 ID 记于此。

来源:
用户会话, 2026-05-30。

## 正文
能力页面里的权限和环境变量这两个东西有什么用？

更底层的问题是: 现在罗列了 Agent 的很多资产。但正在入门 Agent 开发的用户并不知道这些不同资产代表什么、实际是什么东西。

每一个资产页面都需要优化，增加相应的说明提示，并链接到对应官方文档。

设计需要更深入和抽象: Berth 理论上是一个抽象层，之后可能兼容其他 coding agent，不能只按当前单一 agent 的实现细节解释。
